import two_crystal_balls from "./src/day1/TwoCrystalBalls";
let idx = Math.floor(Math.random() * 10000);
const data = new Array(10000).fill(false);

for (let i = idx; i < 10000; ++i) {
        data[i] = true;
}


two_crystal_balls(data);

